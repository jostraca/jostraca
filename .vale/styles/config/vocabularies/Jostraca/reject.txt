# The banned list. Entries are REGEXES, matched case-insensitively by
# Vale, and each one fails CI as Vale.Avoid.
#
# THIS FILE IS THE SINGLE SOURCE OF TRUTH. ts/test/docs.test.ts reads it
# rather than holding its own copy, so the fast local gate and the CI
# prose gate cannot disagree. Add a phrase here and both pick it up.
#
# Sources: the original house list, plus claudisms.ai. Entries from that
# list which collide with this project's own vocabulary are deliberately
# NOT here -- see "What is not banned, and why" in docs/STYLE-GUIDE.md.

# --- Filler and false emphasis -----------------------------------------
worth noting
important to note
it cannot be overstated
at its core
when it comes to
let'?s break it down
here'?s where it gets interesting
the point is
because it matters

# --- Inflated vocabulary -----------------------------------------------
delve
dive into
robust
seamless(?:ly)?
comprehensive(?:ly)?
holistic
intricate
leverag(?:e|es|ed|ing)
foster(?:s|ed|ing)?
shed(?:s|ding)? light on
pav(?:e|es|ed|ing) the way
pivotal
transformative
game.chang(?:er|ing)
cutting.edge
groundbreaking
testament to
paradigm shift
realm
landscape (?:of|for)
underscor(?:es|ing) the
lean(?:s|ing)? (?:in)?to
throughline
double.click on
mature setup

# --- Consultant register -----------------------------------------------
north star
key takeaways
best practices
at the end of the day
pressure.test
right.size
strategic imperative
three things to (?:know|understand)
dispatches from
best operators
lessons learned

# --- Metaphor inflation ------------------------------------------------
load.bearing
heavy lifting
is doing the work
different physics
rules of physics
(?:hits|lands|strikes) hardest
quietly

# --- The contrast frame and its cousins --------------------------------
not just
not only .{0,40}\bbut\b
it'?s not about
the whole game
the entire point
the only thing that (?:matters|changed)

# --- False singularity and crowned superlatives ------------------------
the right (?:way|answer|tool|question)
the best thing you can do
if I had to pick
what (?:struck|got) me
stuck with me
struck a chord
(?:hit|struck) a nerve
we'?ve seen this movie
we'?ve been here before

# --- Reflective pose ---------------------------------------------------
sit with
worth (?:exploring|considering|a look|asking|talking about)
keeps? coming back to
that'?s the tell
where I landed

# --- Invented observation about people ---------------------------------
most people
everyone I'?ve worked with
a lot of folks
nobody I know

# --- Requires approval per use -----------------------------------------
#
# Not filler in the way the rest of this list is: the word is fine in
# ordinary English. It is here because it had become a verbal tic across
# both repositories -- "the honest word", "the honest complication", "the
# corpus that keeps them honest" -- where it was doing no work that the
# surrounding sentence was not already doing. Four uses were removed the
# day this line was added.
#
# There is no inline exemption for it, deliberately: an escape hatch that
# exists gets used. A use the author wants kept is approved by editing
# this line, which is visible in review.
honest(y|ly)?

# The framework the four kinds come from. A fact about how these pages
# were planned, not one a reader needs in order to read them: naming it
# sends somebody looking for how to write a file tree off to learn a
# taxonomy first. Say tutorial, how-to, reference, explanation. The
# style guide and the contributor guides may name it; documentation may
# not, which is why this entry exists.
#
# The middle is `[aá].?` because the accented spelling in these files is
# DECOMPOSED -- a plus a combining acute -- so a pattern written for the
# single-codepoint form silently matches nothing. Found the hard way:
# two greps came back clean over files that plainly contained the word.
di[aá].?taxis

# --- Signposting -------------------------------------------------------
let'?s explore
now let'?s turn to
moving on to
in today'?s rapidly evolving
reflecting a broader trend
marking a significant shift
great question
